Para modificar la web, los cambios se tienen que realizar en el index.html de
la carpeta base. Para indicar que un texto se tiene que traducir se debe poner
la key entre los tags <lang>key</lang>, y la traducción correspondiente en los
ficheros de traduccion que se encuentran en ../assets/locales/*

Finalmente para traducir el index, se ejecuta el script en python encontrado en la carpeta
base.
